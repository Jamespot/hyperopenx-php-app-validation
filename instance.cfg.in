[buildout]
parts = directories app-service publish-connection-parameter

[directories]
recipe = slapos.cookbook:makedirectory
var = ${buildout:directory}/var
etc = ${buildout:directory}/etc
log = ${buildout:directory}/var/log

[app-service]
recipe = slapos.cookbook:wrapper
command-line = php -S ${:host}:${:port} -t ${buildout:parts-directory}/app
wrapper-path = ${directories:etc}/run-php
host = ${slap-network-information:global-ipv6}
port = 8080

[publish-connection-parameter]
recipe = slapos.cookbook:publish
url = http://[${app-service:host}]:${app-service:port}
